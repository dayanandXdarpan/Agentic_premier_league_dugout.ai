# Frontend Environment Variables

# The base URL of the FastAPI backend.
# Locally, it's typically http://localhost:8000 or http://localhost:8080.
# In production, this can be an absolute URL or relative if using the same domain.
VITE_API_URL=http://localhost:8000
